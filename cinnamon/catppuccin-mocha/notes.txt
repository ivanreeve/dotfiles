ivanreeve @ github

oh-my-posh, neofetch(optional) must be invoked on terminal startup

For example (bash):

# add 1 line padding on top
echo ""

# initialize oh-my-posh
eval "$(oh-my-posh init bash --config ~/.cache/oh-my-posh/themes/grapes.omp.json)"

# run neofetch on terminal startup
neofetch
